services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/var/www
      - ./storage/app/firebase_auth.json:/app/storage/app/firebase_auth.json
    ports:
      - "9000:9000"
    env_file:
      - .env
    


